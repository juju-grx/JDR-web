<form method="post" action="{{ path('caracter_delete', {'id': caracter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ caracter.id) }}">
    <button class="btn">Delete</button>
</form>
