<form method="post" action="{{ path('type_class_delete', {'id': type_class.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_class.id) }}">
    <button class="btn">Delete</button>
</form>
